{
  "name": "estoque-agape",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "backend": "cd backend && json-server --watch db.json --port 3001",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Sistema de controle de estoque para fábrica de bolachas",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^1.0.0-beta.13"
  }
}
