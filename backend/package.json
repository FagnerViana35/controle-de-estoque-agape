{
  "name": "estoque-agape-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port ${PORT:-3001} --watch db.json",
    "dev": "json-server --host 0.0.0.0 --port 3001 --watch db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.13"
  }
}
